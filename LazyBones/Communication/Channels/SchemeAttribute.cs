using LazyBones.Config;

namespace LazyBones.Communication.Channels
{
    class SchemeAttribute : ConfigItemAttribute
    {
        public SchemeAttribute(string name)
            :base(name)
        {
        }
    }
}
